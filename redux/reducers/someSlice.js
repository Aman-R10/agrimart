// redux/reducers/someSlice.js
import { createSlice } from '@reduxjs/toolkit';

const someSlice = createSlice({
  name: 'someSlice',
  initialState: {},
  reducers: {},
});

export default someSlice.reducer;
